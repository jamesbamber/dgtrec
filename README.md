# dgtrec

A simple perceptron neural network used to recognize digits from the MNIST database.

## Dataset

The dataset I'm using (and its file format) can be found [here](https://github.com/cvdfoundation/mnist).